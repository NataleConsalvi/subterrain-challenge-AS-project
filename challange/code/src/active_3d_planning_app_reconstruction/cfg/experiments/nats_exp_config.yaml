# Experiment (Drone in cave) specific parameters

# Bounding boxes (in initialization frame, relative to PlayerStart)
map_bounding_volume:
  x_min: -500.0
  x_max: 500.0
  y_min: -500.0
  y_max: 500.0
  z_min: -500.5
  z_max: 500.0

target_bounding_volume:
  x_min: -500
  x_max: 500
  y_min: -500
  y_max: 500
  z_min: -500
  z_max: 500

# System Constraints
planner:
  system_constraints:
    v_max: 1.0
    a_max: 1.0
    yaw_rate_max: 1.57
